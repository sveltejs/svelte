# crag:auto-start
# Generated from governance.md by crag
# Regenerate: crag compile --target pre-commit
repos:
  - repo: local
    hooks:
      - id: gate-1
        name: "lint: npm run lint"
        entry: bash -c 'npm run lint'
        language: system
        pass_filenames: false
        always_run: true
      - id: gate-2
        name: "lint: npx tsc --noEmit"
        entry: bash -c 'npx tsc --noEmit'
        language: system
        pass_filenames: false
        always_run: true
      - id: gate-3
        name: "test: npm run test"
        entry: bash -c 'npm run test'
        language: system
        pass_filenames: false
        always_run: true
      - id: gate-4
        name: "build: npm run build"
        entry: bash -c 'npm run build'
        language: system
        pass_filenames: false
        always_run: true
      - id: gate-5
        name: "ci (inferred from workflow): pnpm test"
        entry: bash -c 'pnpm test'
        language: system
        pass_filenames: false
        always_run: true
      - id: gate-6
        name: "ci (inferred from workflow): pnpm test runtime-runes"
        entry: bash -c 'pnpm test runtime-runes'
        language: system
        pass_filenames: false
        always_run: true
      - id: gate-7
        name: "ci (inferred from workflow): pnpm check:tsgo"
        entry: bash -c 'pnpm check:tsgo'
        language: system
        pass_filenames: false
        always_run: true
      - id: gate-8
        name: "ci (inferred from workflow): pnpm check"
        entry: bash -c 'pnpm check'
        language: system
        pass_filenames: false
        always_run: true
      - id: gate-9
        name: "ci (inferred from workflow): pnpm lint"
        entry: bash -c 'pnpm lint'
        language: system
        pass_filenames: false
        always_run: true
      - id: gate-10
        name: "ci (inferred from workflow): }"
        entry: bash -c '}'
        language: system
        pass_filenames: false
        always_run: true
      - id: gate-11
        name: "ci (inferred from workflow): pnpm build"
        entry: bash -c 'pnpm build'
        language: system
        pass_filenames: false
        always_run: true
      - id: gate-12
        name: "contributor docs (advisory — confirm before enforcing): p..."
        entry: bash -c 'pnpm check:watch  # from CONTRIBUTING.md'
        language: system
        pass_filenames: false
        always_run: true

# crag:auto-end
