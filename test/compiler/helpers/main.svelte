<p>{{reverse('backwards')}}</p>

<script>
	export default {
		helpers: {
			reverse ( str ) {
				let reversed = '';
				let i = str.length;
				while ( i-- ) reversed += str[i];
				return reversed;
			}
		}
	};
</script>
