<script>
	export let numbers;

	function foo(node, params) {
		return {
			duration: 100,
			tick: t => {
				node.foo = t;
			}
		};
	}
</script>

{#each numbers as num, i (num)}
	<div transition:foo>{num}</div>
{/each}