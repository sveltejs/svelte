{
	"extends": "../../tsconfig.json",
	"include": [".", "../ambient.ts"],

	"compilerOptions": {
		"lib": ["ESNext", "dom", "dom.iterable"],
		"target": "ESNext",
		"types": [],

		"baseUrl": ".",
		"paths": {
			"svelte/*": ["*"]
		}
	}
}
